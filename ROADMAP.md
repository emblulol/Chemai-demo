# ChemAI 平台改造 · Roadmap

> 面向真实教学流程的 UI 重构与身份/环境合理化。本文档是后续改造的工作基线。
> 更新日期：2026-09-01
>
> **范围调整（2026-08-28）**：移除 ⑦ 学习通适配（不实施），聚焦 ①~⑥。实施已开始。

---

## 一、背景与目标

ChemAI 是面向大学化学实验教学（三草酸合铁酸钾制备实验）的 AI 平台，现有 7 个页面（index / assistant / main / knowledge / corpus / prep / generator）+ 7 个运行时数据文件（FAQ / 语料 / 图谱 / 讲义 / 图片 / 题库 / 报告细则）。

**改造总目标**：

1. 贴合**真实教学流程**（学生「预习→实验→答疑→测评→复习」，教师「备课→命题→授课→评估→追踪」）。
2. **减少重复性点击**（模式切换、身份往返、新标签页、侧栏折叠等）。
3. **师生零门槛上手**，轻松满足教学与学习两类任务。

---

## 二、当前进展（已完成）

| 版本 | 内容 | 涉及文件 |
|------|------|---------|
| v75 | 数据自洽 + 主题跨页继承 + 图谱手机端优化 | `kg.json` / `corpus.json` / `academic_lexicon.json` / `index.html` / `knowledge.html` |
| v72 | 助手代码清理 + 置信度口径 + 知识图谱深链 | `assistant.html` / `knowledge.html` / `agent-cluster.js` |
| v72.1 | agent 管道修复：LLM 补质检、qaBusy 时序+按消息隔离、技能卡时序、小清理 | `assistant.html` |

详见 `CHANGELOG.md`。

### 数据体检结论（v75 已改数据）

- KG 123 节点 / 195 边：层级、类目分解加总自洽（v75 修 9 个 `level:4`→`level:3`、清 15 个死字段 `cat`）。
- corpus 445 条目：子域/类型/语言求和自洽（v75 修 `difficulty` 中→中级×15、`doctype` 科普/讲义→科普×1）。
- academic_lexicon：`per_subfield` 去重后加总 = total（13092/1440）。
- FAQ 3102 条：`knode` 100% 空、`manual_refs` 100% 空、`ents` 48% 空、`detail` 47% 空——内容质量高，缺的是「量」，需数据回填（独立于 UI 改造）。
- **遗留缺口**：15 条语料缺 `depth`/`datatype`（id 277–291）、89 条缺 `questions`（id 367–455）、1 条缺 `path`（id 367）；KG 缺「配位化学理论 / 安全与废物处理」两子域节点。
- `kb.json`（1335 条）为遗留死文件（BOM、运行时零引用），可归档或删除。

---

## 三、7 大支柱改造方向

### ① 角色化工作台
- 首页按身份聚合入口链：学生见「预习→实验→答疑→测评→复习」，教师见「备课→命题→授课→评估→追踪」。
- 导航按角色过滤（现有 3 个 `data-role-gate` 之外，其余入口按需隐藏）。

### ② 教学流程步骤条
- 首页 / 助手顶部加 Stepper，学生、教师各一条，点击直达对应页面 / Tab。

### ③ 控制收敛
- 现状三层概念并存：`6 工作模式 pills` × `正常/集群 switch` × `身份`。
- 收敛为一套主入口：默认「直接问答」，深度/研究/测评/复习作为明确分页动作；集群改为「按需自动联网 + 一个手动开关」。

### ④ 一键直达 + 站内跳转
- 深链/文献卡/图谱卡由 `target="_blank"` 改**站内跳转**（学习通 webview 硬约束，也减少标签页堆积）。
- 4 部本地视频去掉侧栏双层 `<details>` 折叠，直接内嵌可播（`preload="none"`）。
- 高频动作（记错题/导出/测评/复习/看视频）收进**固定快捷条**。
- 常用配置（LLM 等）上移，减少进侧栏翻找。

### ⑤ CSS 技术债清理
- `assistant.html` 现有 4+ 个 `<style>` 块 + `polish-v63`/`model-v1`/`role-style` 追加覆盖，同选择器重复定义。
- 合并为单一主题源，删死样式（`.role-bar`/`.rs-chip` 等已确认无 DOM），统一配色/间距/动效令牌。

### ⑥ 身份验证与用户识别记录
- **现状问题**：身份是 `localStorage['chem-user'].state.isLoggedIn` 纯前端伪登录，可随意伪造「教师」；所有记录（错题本 `chemaiWrongBook`、掌握度 `chemai_mastery_v1`、SRS `chemai_srs_v1`、收藏/笔记/反馈）是**设备级** key，不区分用户，同机多人串数据。
- **合理落法**：
  1. 用户档案模型 `{ uid, name, role, createdAt }`（uid 用 `crypto.randomUUID()`）。
  2. 记录按用户命名空间隔离：`chemai_<uid>_...`，切换用户即切换数据视图。
  3. 诚实「角色选择」替代伪 `isLoggedIn`，教师功能加「无密码、不验证真实身份」明确提示。
  4. 封装 `auth` 模块，预留服务端认证接口位（未来换 OAuth/JWT 不动上层）。
  5. 学习画像导出 / 错题本 / 报告评估带上 `uid/name/role` 头，使记录可归属。

### ~~⑦ 嵌入学习通适配~~（已移除，不实施）
- **可行性**：纯静态 H5 可通过学习通「网页章节」iframe / webview 加载。
- **与 ⑥ 强耦合**：学习通提供权威身份（学号/姓名/角色），用学习通身份替代本地伪造身份。
- **适配清单**：
  1. 身份优先从学习通 URL 参数（`?uid=&name=&role=`）或 JS-SDK 读取。
  2. 全部 `window.open`/`target="_blank"` 改站内跳转（webview 无新标签）。
  3. localStorage 降级：加内存兜底 + 记录键挂学习通 uid。
  4. 移动端（≤640px）优先，学习通主要在手机 App。
  5. 外部资源（DeepSeek/PubChem/维基/Bing/视频）在 webview 内可用性需实测。

---

## 四、分阶段路线图

| 批次 | 内容 | 落地页 | 验证 |
|------|------|--------|------|
| **第一批** | ⑥ 身份/记录 + ④ 站内跳转 + ③ 控制收敛 | `assistant.html` 示范 | `run` 截图对比、改前/改后走查 |
| **第二批** | ② 步骤条 + ① 角色化工作台 | 首页入口 + 全站导航 | 角色切换 / 流程跳转走查 |
| **第三批** | ⑤ CSS 合并 + `simplify` 审查 + 全站移动端/可访问性 | 全站 | 移动端 / 对比度 / 焦点环复查 |

**依赖顺序**：⑥⑦（地基）→ ①（依赖 ⑥）→ ④（服务 ⑦）→ ⑤（收尾）。

---

## 五、关键风险与约束

1. **`index.html` 是 React 编译产物**（`assets/index-B-pT4Snc.js`，源码未随仓库维护）→ 建议**暂不纳入**，角色工作台落在可维护 HTML / 注入脚本。
2. **无后端 → 认证上限**：只能做到「前端角色 + 明确边界 + 按用户隔离」；真密码认证需 Serverless 后端，另立项目。
3. **学习通环境未知**：iframe 是否允许、身份参数名、`localStorage` 是否持久、`window.open` 是否可用、外部 API 网络策略——**需查证或由业务方提供对接说明**。
4. **记录持久性**：学习通 webview 可能清 localStorage，需「内存兜底 + 明确提示」。

---

## 六、待拍板事项（阻塞开工）

- [ ] **先做哪批？**（建议第一批 = ⑥⑦④③，落 `assistant.html`）
- [ ] **`index.html` 纳入否？**（建议暂不纳入）
- [ ] **学习通对接**：WebSearch 查证，或由业务方提供对接文档

---

## 七、远期展望

### 近期（数据回填 + 技术债清理）
- **数据回填**：补齐 15 条 `depth`/`datatype`、89 条 `questions`、1 条 `path`；KG 补「配位化学理论 / 安全与废物处理」两子域节点。
- **技术债**：清理根目录 ~174 个 `(2)` 陈旧副本（约 13.7MB）；`kb.json` 死文件归档/删除。

### 远期（本次不实施）
- **真认证**：Serverless 后端（Cloudflare Worker / Vercel Function）+ JWT/OAuth，接入学习通 / 学校统一身份。
- **记录云端化**：按学习通 uid 把错题本/测评/复习/画像存服务端，跨设备同步。
- **学习通深度对接**：JS-SDK 回传测评成绩/报告到课程，形成「平台作答 → 学习通计分」闭环。
